import { Meta } from "@storybook/addon-docs/blocks";
import { CodeSpecimen } from "./specimens/specimens";

<Meta title="Foundations/Code" />

# Code

The syntax palette Pathfinder shipped, extracted verbatim: nine roles, no cool hue,
every token measured against the code ground it sits on. The hierarchy runs quiet to
loud — comments, punctuation, plain text, names, constants, strings, keywords,
functions.

<CodeSpecimen />
