import { Meta } from "@storybook/addon-docs/blocks";
import { BreakpointSpecimen } from "./specimens/specimens";

<Meta title="Foundations/Breakpoints" />

# Breakpoints

Reconciled from the widths the shipped surfaces already used. Breakpoints cannot be
custom properties inside `@media`, so they ship as constants and as this list.

<BreakpointSpecimen />
